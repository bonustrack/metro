{
  "comment": "The MCP server entry (src/mcp/index.ts) is run directly by Bun and is excluded from the daemon's tsconfig (it pulls in the MCP SDK + Web/DOM globals, which the daemon build does not need). It is typechecked here on its own, mirroring how the station entrypoints are bun-run and kept out of the compiled daemon build.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "lib": ["ES2022", "DOM"],
    "types": ["node", "bun"]
  },
  "include": ["src/mcp/index.ts"]
}
