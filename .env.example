# For apps/api (api.metro.box). A daemon run with `metro serve` needs none of this.

# Required
DATABASE_URL=postgres://user:pass@host:5432/metro
WORKOS_API_KEY=sk_test_...
WORKOS_CLIENT_ID=client_...

# Optional
# METRO_LOG_LEVEL=info
