DATABASE_URL=postgres://user:pass@host:5432/metro
# METRO_AGENT=1
# METRO_MCP_HTTP_TOKEN=
# METRO_LOG_LEVEL=info
# Google sign-in for the web UI (server-side OAuth authorization-code flow).
# One Google "Web" OAuth client; redirect URI https://mcp.metro.box/auth/google/callback.
# GOOGLE_OAUTH_CLIENT_ID=xxxxx.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_SECRET=xxxxx
# Grant a verified Google email access to operator-provisioned agents (owner_email IS
# NULL) by name; the names are resolved to agent ids at sign-in and never match an agent
# somebody owns. Self-owned agents need no entry here. Names match exactly, casing
# included, and agents.name is not unique, so a name shared by two operator rows grants
# both (read-only: never a key, never a delete).
# GOOGLE_EMAIL_AGENTS={"fabien@bonustrack.co":["Tony"]}
# Secret used to sign the OAuth state and the daemon session JWT (HS256). Keep long/random.
# METRO_SESSION_SECRET=change-me-to-a-long-random-string
# Sign-in is open: any Google account with a verified email may sign in, create agents and
# delete the agents it owns. There is no domain allowlist and no per-owner agent cap.
# Optional overrides: GOOGLE_OAUTH_REDIRECT_URI, METRO_SESSION_TTL_SEC (default 30d).
